meta {
  name: InkFlow API
  type: collection
  version: 1
}

docs {
  # InkFlow API (Bruno)

  Import full spec: **Bruno → Import → OpenAPI** → select `docs/openapi.yaml`.

  Or use requests below as starters. Set `Staging` environment.

  See [MOBILE_API.md](../MOBILE_API.md).
}
